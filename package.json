{
  "name": "website",
  "packageManager": "yarn@4.1.0",
  "devDependencies": {
    "express": "^4.18.3",
    "prettier": "^3.2.5"
  },
  "scripts": {
    "start": "node server.js",
    "build": "mkdir -p dist/src && cp -r src/ dist/src && cp index.html dist/ && echo 'Build complete!'"
  },
  "dependenciesMeta": {
    "prettier@3.2.5": {
      "unplugged": true
    }
  }
}
